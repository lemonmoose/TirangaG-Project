# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/tiranga-solar

# JWT Secret (Change this to a strong random string in production!)
JWT_SECRET=your-jwt-secret-key-change-in-production-use-strong-random-string

# Server Configuration
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Email Configuration (Optional - for sending email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# WhatsApp API (Optional - for WhatsApp notifications)
# WHATSAPP_API_KEY=your-whatsapp-api-key
# WHATSAPP_PHONE=your-whatsapp-number
